/**
 * @param {number[]} nums
 * @return {number[][]}
 */
var permute = function (nums) {
  let result = [];

  let index = 0;

  let end = (nums.length - 1) * nums.length;

  while (result.length !== end) {
    let temp = [];
    temp.push();
  }
};
